# FreeTier
Handles retrieval of free tier information from GCP documentation.

- `models.py`: Data models and regex patterns for extraction.
- `service.py`: `FreeTierService` for scraping and caching free tier data.
